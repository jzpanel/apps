services:
  node:
    image: jzpanel/node:{{tag}}
    container_name: panel_node_16
    restart: unless-stopped
    network_mode: host
    volumes:
      - /www/wwwroot:/www/wwwroot
      - /www/server/node_16/supervisor:/etc/supervisor/conf.d
      - /www/logs/node_16:/var/log/supervisor
      - /www/wwwlogs:/www/wwwlogs
    deploy:
      resources:
        limits:
          memory: {{mem_limit}}
          cpus: "{{cpu_limit}}"
        reservations:
          memory: {{mem_reserve}}
